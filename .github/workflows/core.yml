name: Core Tests
on: push
jobs:
  tests:
    name: Core tests
    runs-on: windows-latest
    steps:
    - uses: actions/setup-python@main
      with:
        python-version: pypy-3.11
        check-latest: true
    - run: pip install -U pip==26.1.2
    - run: pip install -U pip
