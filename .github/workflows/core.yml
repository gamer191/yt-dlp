name: Core Tests
on: push
jobs:
  tests:
    name: Core tests
    runs-on: windows-latest
    steps:
    - uses: actions/setup-python@main
      with:
        python-version: pypy-3.11
    - run: python -m pip install -U pip==26.1.2
    - run: pip
